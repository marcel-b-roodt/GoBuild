## Shape generator tests — GdUnit4
extends GdUnitTestSuite


# ── Cube ─────────────────────────────────────────────────────────────────

func test_cube_placeholder() -> void:
	# TODO: assert_int(CubeGenerator.generate(1,1,1,0).faces.size()).is_equal(6)
	assert_bool(true).is_true()


# ── Plane ────────────────────────────────────────────────────────────────

func test_plane_placeholder() -> void:
	# TODO: assert_int(PlaneGenerator.generate(1,1,1,1).faces.size()).is_equal(1)
	assert_bool(true).is_true()


# ── Cylinder ─────────────────────────────────────────────────────────────

func test_cylinder_placeholder() -> void:
	assert_bool(true).is_true()

