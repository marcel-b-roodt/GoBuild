
# ── Internal planning docs (local-only, not published with the addon) ──
docs/
TODO.md
NearTermTodos.txt
HighLevelTodos.txt

# ── Godot ──────────────────────────────────────────────────────────────
*.stex
*.texarr
/.mono
/.godot
/.export_build

# ── Third-party addons (install from AssetLib) ──────────────────────────
# GdUnit4 — testing framework. Install via Project → AssetLib → "GdUnit4".
addons/gdUnit4/

# ── C# / .NET build outputs ────────────────────────────────────────────
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/
.vscode/

# ── NuGet ───────────────────────────────────────────────────────────────
*.nupkg
*.snupkg
.nuget/
packages/
# Restore outputs (nuget.config may redirect these, but cover the defaults)
[Pp]ackages/
project.lock.json
project.fragment.lock.json
artifacts/

# ── Compiled output ─────────────────────────────────────────────────────
*.dll
*.exe
!icon.ico
*.pdb
*.mdb

# ── IDE ─────────────────────────────────────────────────────────────────
.idea/
*.iml

# ── OS cruft ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Audio workfiles ─────────────────────────────────────────────────────
*.reapeaks
*.rpp-bak