
# ── Internal planning docs (local-only, not published with the addon) ──
docs/internal/
AGENTS.md
.github/copilot-instructions.md
opencode.json
.opencode/
issues/
TODO.md
NearTermTodos.txt
HighLevelTodos.txt

# ── GdUnit4 test reports (local-only, generated on each test run) ───────
reports/

# ── Godot ──────────────────────────────────────────────────────────────
*.stex
*.texarr
/.mono
/.godot
/.export_build
/gobuild_export/

# ── Third-party addons (install from AssetLib) ──────────────────────────
# GdUnit4 — testing framework. Install via Project → AssetLib → "GdUnit4".
addons/gdUnit4/

# ── C# / .NET build outputs (kept in case GDExtension is added later) ──
[Bb]in/
[Oo]bj/
*.user
*.suo
.vs/

# ── IDE ─────────────────────────────────────────────────────────────────
.idea/
*.iml

# ── OS cruft ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Audio workfiles ─────────────────────────────────────────────────────
*.reapeaks
*.rpp-bak

# ── VSCode ──────────────────────────────────────────────────────────────
.vscode/settings.json
